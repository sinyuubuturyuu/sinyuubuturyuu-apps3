rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 本番/ローカル共通: Firestore はログイン済みユーザーのみ許可します。
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
